const fs=require('fs');let c=fs.readFileSync('src/App.tsx','utf8');if(!c.includes('FocusTimer')){c=c.replace('const AIAssistant','const FocusTimer = lazy(() => import("./pages/timer/FocusTimer"))\nconst AIAssistant');c=c.replace('<Route path="/ai"','<Route path="/timer" element={<FocusTimer />} />\n          <Route path="/ai"');fs.writeFileSync('src/App.tsx',c);console.log('Done!');}else{console.log('Already added');}
